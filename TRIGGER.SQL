-- ============================================
-- Topic: SQL TRIGGERS
-- Description: Automatic actions in database
-- ============================================

-- Step 1: Create Employees table
CREATE TABLE Employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    salary INT
);

-- Step 2: Create Audit table to store changes
CREATE TABLE Salary_Audit (
    audit_id INT AUTO_INCREMENT PRIMARY KEY,
    emp_id INT,
    old_salary INT,
    new_salary INT,
    change_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Step 3: Insert sample data
INSERT INTO Employees VALUES
(1, 'Rahul', 50000),
(2, 'Priya', 45000),
(3, 'Amit', 60000);

-- ============================================
-- BEFORE UPDATE TRIGGER
-- This trigger stores old and new salary
-- whenever salary is updated
-- ============================================

DELIMITER //

CREATE TRIGGER before_salary_update
BEFORE UPDATE ON Employees
FOR EACH ROW
BEGIN
    INSERT INTO Salary_Audit(emp_id, old_salary, new_salary)
    VALUES(OLD.emp_id, OLD.salary, NEW.salary);
END //

DELIMITER ;

-- ============================================
-- Update salary to test the trigger
-- ============================================

UPDATE Employees
SET salary = 55000
WHERE emp_id = 1;

-- ============================================
-- View audit records generated by trigger
-- ============================================

SELECT * FROM Salary_Audit;
